# Exit codes

TODO: Trivy and OSV-Scanner exit code meanings and how they are normalized.
Note that OpenGrep and Hadolint use their own severity flags directly
rather than exit code normalization, since they follow the rule-severity
gate model, not the CVSS gate model.
