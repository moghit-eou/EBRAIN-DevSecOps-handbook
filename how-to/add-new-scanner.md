# How to add a new scanner

TODO: what a new tool needs to output to slot into the existing gate logic.
Note which gate model it should join, CVSS score or rule severity, since
that decision changes how its output gets parsed.
